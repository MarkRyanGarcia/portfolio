export type navBarLink = {
    label: string
    route: string
}

export type ExperienceItem = {
    title: string
    company: string
    timeline: string
    location: string
    bullets: string[]
}